{{> header}}
<div class="container">
    <div class="row">
        {{#each itemsGaleria}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-2 pb-3">
                <img src="{{this}}" class="img-fluid rounded" alt="">
            </div>
        {{/each}}
    </div>
</div>
{{> footer}}